<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- State when a row is being pressed, but hasn't yet been activated (finger down) -->
    <item android:drawable="@color/afterSelect1" android:state_pressed="true" />

    <!-- Used when the view is "activated". -->
    <item android:drawable="@color/afterSelect" android:state_activated="true" />

    <!-- Default, "just hangin' out" state. -->
    <item android:drawable="@android:color/transparent" /></selector>