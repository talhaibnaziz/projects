#include <bits/stdc++.h>

using namespace std;

int main()
{
    long long ts, tf, t, arr[100005];
    int n;
    scanf("%lld %lld %lld", &ts, &tf, &t);
    scanf("%d", &n);
    for(int i=0; i<n; i++)
    {
        scanf("%lld", &arr[i]);
    }
    long long time;
    time = ts;
    int idx = 0;
    while(idx<n)
    {

    }

    return 0;
}

